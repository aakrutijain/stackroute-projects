spring:
  datasource:
    url: jdbc:mysql://localhost:3306/keepnotedb
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
